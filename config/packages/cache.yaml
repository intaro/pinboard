framework:
    cache:
        # Filesystem cache — works out of the box, no extra services required.
        # To switch to Redis: uncomment the two lines below and set REDIS_URL in .env.local
        #app: cache.adapter.redis
        #default_redis_provider: '%env(REDIS_URL)%'

        # APCu — fast in-process cache, requires the apcu PHP extension
        #app: cache.adapter.apcu
