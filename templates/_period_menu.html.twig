<ul class="nav nav-pills small period">
    {% for label in periods %}
        <li class="nav-item">
                                                                    {# Опять же format тут не должен быть, поправить #}
            <a class="nav-link {{ period == label ? 'active' : '' }}" href="{{ path('server', { serverName: server_name, hostName: hostname, period: label, format: 'html' }) }}">
                {{ label }}
            </a>
        </li>
    {% endfor %}
</ul>
